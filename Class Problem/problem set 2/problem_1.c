#include <stdio.h>

int main()
{
    int n;
    scanf("%d", &n);
    if (n > 20)
    {
        printf("Eligible for marriage.");
    }
    else
    {
        printf("Not eligible for marriage.");
    }
    return 0;
}